{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package (queue_system)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go", 
            "envFile": "${workspaceFolder}/.env",        
            "args": [],                                  
            "showLog": true,                             
            "dlvFlags": ["--check-go-version=false"]    
        }
    ]
}